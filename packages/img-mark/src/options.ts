export interface MarkdownItImgMarkOptions {
  /**
   * Lightmode only ids
   *
   * 日间模式 ID
   *
   * @default ["light"]
   */
  light?: string[];

  /**
   * Darkmode only ids
   *
   * 夜间模式 ID
   *
   * @default ["dark"]
   */
  dark?: string[];
}
